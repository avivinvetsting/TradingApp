# Core runtime dependencies (Python 3.11)
# Pin ranges chosen for stability and widewheel availability on Linux/WSL2

# Broker & connectivity (pinned for stability)
ib-insync==0.9.86

# Config & CLI (pinned)
pydantic==2.11.7
python-dotenv==1.1.1
PyYAML==6.0.2
typer==0.16.1

# Data & computing (pinned)
numpy==2.3.2
pandas==2.3.1
pyarrow==17.0.0
pandas-market-calendars==5.1.1
pydantic-settings==2.10.1
requests==2.32.5

# Indicators & analytics
ta==0.11.0

# Reporting (pinned to avoid WSL issues)
plotly==5.18.0
# Static image export for Plotly on WSL2 (headless)
kaleido==0.2.1
Jinja2==3.1.6

# Historical data fallback for corporate actions factors (optional but recommended)
yfinance==0.2.65

# --- Development & QA (pinned) ---
pytest==8.4.1
pytest-asyncio==0.23.8
pytest-cov==6.2.1
mypy==1.17.1
ruff==0.12.11
black==24.10.0
pre-commit==3.8.0
ipykernel==6.30.1
jupyterlab==4.4.6
